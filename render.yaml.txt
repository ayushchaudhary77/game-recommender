services
  - type web
    name game-recommender
    env python
    buildCommand pip install -r requirements.txt
    startCommand gunicorn appapp
    envVars
      - key PYTHON_VERSION
        value 3.11.0